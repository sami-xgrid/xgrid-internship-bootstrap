version: '3.8'
services:
  backend:
    build: ./backend
    ports:
      - "5000:5000"
    environment:
      - DB_HOST=${DB_HOST}
      - DB_USER=dbadmin
      - AWS_REGION=ap-south-1

  frontend:
    build: ./frontend
    ports:
      - "80:80"
    depends_on:
      - backend
